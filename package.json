{
  "name": "the-button",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc && vite build",
    "preview": "vite preview --host",
    "verify": "tsc --noEmit",
    "vo": "bun scripts/generate-vo.ts"
  },
  "dependencies": {
    "three": "^0.160.0"
  },
  "devDependencies": {
    "@types/three": "^0.160.0",
    "kokoro-js": "^1.2.1",
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  }
}
