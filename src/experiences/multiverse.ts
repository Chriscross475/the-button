import { defineReveal } from '../levels/scaffold';
import { revealMultiverse } from '../levels/multiverse';

// The multiverse: a rift into every other version of this room, one rule wrong
// in each, and one prime button somewhere among them.

export const multiverse = defineReveal('multiverse', 1.0, revealMultiverse);
