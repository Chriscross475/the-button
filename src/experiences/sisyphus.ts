import { defineReveal } from '../levels/scaffold';
import { revealSisyphus } from '../levels/sisyphus';

// Sisyphus: a Greek hillside, a boulder with the button on top, and a summit
// the boulder never quite reaches.

export const sisyphus = defineReveal('sisyphus', 1.0, revealSisyphus);
