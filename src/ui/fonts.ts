// The game's type, by role — one stack each, so every sign in every room
// renders the same:
//   VOICE   — the narrator and anything "official": titles, plaques, posters,
//             small print. Deadpan, bookish.
//   SIGN    — plain UI and in-world signage: labels, price tags, screens' text.
//   DISPLAY — machines: scoreboards, counters, keypads, digital readouts.
// (The tutorial's card deliberately uses its own over-friendly font.)
export const FONT_VOICE = 'Georgia, "Times New Roman", serif';
export const FONT_SIGN = 'system-ui, -apple-system, "Segoe UI", Roboto, Arial, sans-serif';
export const FONT_DISPLAY = 'ui-monospace, "Cascadia Mono", Menlo, Consolas, monospace';
