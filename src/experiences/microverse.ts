import { defineReveal } from '../levels/scaffold';
import { revealMicroverse } from '../levels/microverse';

// The microverse: a glass tank on a table, a tiny civilisation inside, and the
// button above them is their sun.

export const microverse = defineReveal('microverse', 1.0, revealMicroverse);
